# fal.ai API key — server-only, never commit the real value
FAL_KEY=

# Anthropic API key — server-only. Optional: powers the ✨ Enhance prompt rewriter
# (Claude Haiku 4.5). Unset = Enhance falls back to the built-in deterministic rewriter.
ANTHROPIC_API_KEY=
